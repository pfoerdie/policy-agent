const
    Assert = require('assert'),
    Context = require("./Context.js"),
    T = require("./tools.js"),
    PDP = {};

T.define(PDP, 'requestDecision', function () {
    // TODO
}); // PDP.requestDecision

module.exports = PDP;